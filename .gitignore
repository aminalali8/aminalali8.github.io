node_modules/
dist/
.astro/
.DS_Store

# src/content/** and public/cv/*.pdf are generated by scripts/sync.mjs but are
# committed on purpose: CI has no vault to sync from, so the committed copy is
# what gets built and deployed.
